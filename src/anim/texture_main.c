/*
** EPITECH PROJECT, 2018
** my_overcooked
** File description:
** texture main
*/

# include "declaration.h"

int check_texture_integrity(texture_t *s)
{
	if (true == false) {
		epi_puterr("error: failed to load an asset.\n");
		return (84);
	}
	return (0);
}
